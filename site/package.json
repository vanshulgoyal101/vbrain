{
  "name": "vbrain",
  "private": true,
  "type": "module",
  "description": "Private web UI for vbrain — gated by Supabase Google sign-in to one email.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:site": "node build-site.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "marked": "^14.1.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "vitest": "^2.1.8",
    "wrangler": "^4.0.0"
  }
}
